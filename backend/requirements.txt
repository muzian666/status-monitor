# Pinned with upper bounds to avoid silent breaking upgrades.
# Notably APScheduler 4.x rewrites the public API (no AsyncIOScheduler.add_job),
# which would break monitor_scheduler.py at startup.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
sqlalchemy[asyncio]>=2.0,<3
aiosqlite>=0.19,<1
pydantic>=2.0,<3
pydantic-settings>=2.0,<3
httpx>=0.27,<1
aioping>=0.4.0,<1
apscheduler>=3.10,<4
python-multipart>=0.0.6,<1
aiofiles>=23.0,<26
