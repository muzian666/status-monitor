apiVersion: v1
kind: Service
metadata:
  name: {{ include "status-monitor.fullname" . }}
  labels:
    app: {{ include "status-monitor.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "status-monitor.name" . }}
